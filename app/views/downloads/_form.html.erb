<%= form_for  [@congress, @download] do |form| %>
  <% if download.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(download.errors.count, "error") %> prohibited this download from being saved:</h2>

      <ul>
      <% download.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form" style="background-color: rgba(255,255,255,0.2);">

  <div class="form-group">
    <%= form.label "Nombre" %>
    <%= form.text_field :name, class: "form-control" , id: :download_name %>
  </div>

  <div class="form-group">
    <%= form.label "Descripción" %>
    <%= form.text_area :description, class: "form-control" , id: :download_description %>
  </div>

  <div class="form-group">
    <%= form.label "Posición" %>
    <%= form.number_field :position, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label "Documento" %>
    <%= form.file_field :file, class: "form-control"  %> 
  </div>

  </div>

  <div class="actions pull-right">
    <%= form.submit "Continuar", class: "btn btn-success" %>
  </div>
<% end %>
