<%= form_for(discussion) do |f| %>
  <% if discussion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(discussion.errors.count, "error") %> prohibited this discussion from being saved:</h2>

      <ul>
      <% discussion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comments_id %>
    <%= f.text_field :comments_id %>
  </div>

  <div class="field">
    <%= f.label :amount_comments %>
    <%= f.number_field :amount_comments %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
