<%= form_for [@congress, @news] do |f| %>
  <% if news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news.errors.count, "error") %> prohibited this news from being saved:</h2>

      <ul>
      <% news.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Título" %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label "Contenido" %>
    <%= f.text_area :content %>
  </div>

  <div class="field">
    <%= f.label "Autor (*** Poner usuario ***)" %>
    <%= f.text_field :author_id %>
  </div>


  <div class="field">
    <%= f.label :picture %>
    <%= f.file_field :picture %>
    
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
