<h1>Editing Presentation</h1>

<%= render 'form', presentation: @presentation %>
<div class="field">
	<% user = User.find(@presentation.uploader_id.to_i)%>
    <B><I><%= ("Subido por " + user.username) %> </I></B>
</div>
<%= link_to 'Show', @presentation %> 
<%= link_to 'Back', presentations_path %>
